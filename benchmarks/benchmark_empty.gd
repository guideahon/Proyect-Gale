## BenchmarkEmpty — escena mínima de benchmark.
#
# Sin geometría, sin shaders, sin streaming. Sirve como baseline para
# medir el overhead del motor y del runner.

extends Node

func _ready() -> void:
	pass
